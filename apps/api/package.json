{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "migrate:create": "wrangler d1 migrations create kakeibo",
    "migrate:local": "wrangler d1 migrations apply kakeibo --local --persist-to .wrangler/state",
    "migrate:remote": "wrangler d1 migrations apply kakeibo --remote"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250105.0",
    "wrangler": "^4.4.0"
  }
}
